const { default: Axios } = require("axios")

var app = new Vue({
    el: '#root',
    data: {

    },
    methods: {
      
       
    },
    mounted() {
        console.log(location.search);
    }
})